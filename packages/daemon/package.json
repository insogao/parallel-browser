{
  "name": "@backlight/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Backlight daemon: browser supervisor, CDP proxy, activity bus, extension manager.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "check": "tsc --noEmit",
    "test": "pnpm run test:unit && pnpm run test:integration",
    "build": "tsc --noEmit",
    "test:unit": "node --test tests/*-unit.ts && node tests/brand.ts",
    "test:integration": "caffeinate -dis node tests/spike.ts && caffeinate -dis node tests/supervisor.ts && caffeinate -dis node tests/extensions.ts && caffeinate -dis node tests/agent.ts && caffeinate -dis node tests/background.ts && caffeinate -dis node tests/usability.ts",
    "test:window-state": "caffeinate -dis node tests/window-state-throttle.ts",
    "test:bg-rebound": "caffeinate -dis node tests/bg-rebound-probe.ts"
  },
  "dependencies": {
    "@puppeteer/browsers": "^3.2.2",
    "chokidar": "^4.0.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2"
  }
}
