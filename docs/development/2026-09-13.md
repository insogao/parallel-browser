# 2026-09-13 开发记录

## 修复与新增

- 品牌图标运行时覆盖；托盘识别品牌 bundle、跨同品牌实例重开、最小化与贴角恢复。
- 人工接手暂停捕获，异步捕获取消、超时及上下文重置；rAF 单调度链。
- 原生扩展侧栏、定向热重载、网页通信示例、DevTools 代理握手缓冲。
- Dashboard、CLI 与本地 demo 更新。

## 验证约束变更

用户要求所有浏览器测试只能在 Backlight 中操作。此前混合引擎测试通过仅是历史诊断，不计本轮验收。

已统一八个浏览器测试入口，必须使用已安装品牌化 Backlight.app 的相同构建副本（校验 executable/icon SHA256），独立资料目录；无 Chrome 回退。副本仍为 Backlight.app，独立应用路径用于避免用户日常托盘截获测试激活。

## 已有证据

- 已安装 Backlight 的 macOS open -a 重开：托盘将激活转到受管实例，恢复正常窗口；未模拟物理鼠标点击。
- 用户确认图标修复；运行中图标保存 artifacts/backlight-running-icon.png。
- 2026-09-13 修改测试入口后 `pnpm -r --if-present run check` 通过。
- Backlight 专用全套测试已完成（结果与日志见下方 OpenCode 补充，最终完整复跑 2026-09-13 19:23 CST：unit 23/23、集成全 PASS）。

## OpenCode 会话补充（TODO 1–4，2026-09-13 傍晚）

### 修复与提交

- `6a4b097 capture`：人工接手发生在 controller 已激活之后（含成功后暂停的竞态）时，若 controller 仍是当前展示，取消路径恢复原网页；用户在竞态中选择的新标签保持不变。清理探测有界并吞掉断连错误；新增 3 个单元断言。
- `2b96f94 ext-dev`：先用 `Browser.getWindowForTarget` 解析请求网页的 windowId，`chrome.sidePanel.close/open` 始终显式使用该 ID；临时 bridge 只提供 userGesture。面板归属用面板内 `chrome.windows.getCurrent()` 验证（实测 `runtime.getContexts` 的 windowId 为 -1，`Browser.getWindowForTarget` 不支持侧栏 target）。隐藏面板的 WebContents 会在 close/open 间复用，因此按归属而非“新 target”识别；单窗口、storage、DevTools 回归通过，双窗口集成测试 3/3 无串窗。
- `5057d2e windows`：Backlight 专用最小化根因与修复（见下）。
- `c85fe7f cdp`：`Cdp.send` 先注册 waiter 再 `ws.send`，同步 send 异常转为 reject，避免“快速响应在 send 期间到达被丢弃”造成永久挂起；`tests/agent.ts` 的全部 CDP 调用加 15s 上限（生产不设短超时，长截图等仍可等待）。
- `f5e9651 agent-test`：角落窗口不产生合成帧，`Page.captureScreenshot` 默认参数会永久等待；代理测试改为可见启动（与 spike 基线一致）并做有界页面发现。
- `358d930 windows`（P2 复审修复）：
  - 假性 minimized：`settleRepeat=true` 时不再信任初始 CDP minimized 报告（隐藏应用上该报告可为假），强制 normal→minimized 恢复循环；重复 `/api/bg` 因此能修复，`tests/windows-unit.ts` 新增对应确定性测试。
  - 最后意图生效：`collapseAll`/`restoreAll` 各自开启控制代际，`minimizeWindow` 在每个延迟步骤前、`/api/bg` 在最终 `hideBrowser` 前校验代际；`/api/show`、`/api/restore`、`/api/launch`、`/api/extensions/dev`、`/api/inspect` 会作废在途的 bg。`appState`/`hideBrowser` 改为可注入 ServerDeps，`tests/proxy-race-unit.ts` 用 API 级竞态证明 show 期间 bg 不最小化、不隐藏、窗口保持可见。
- `0bf8bd2 control`（P2 并发复审第二轮的修复）：
  - `/api/bg` 在任何 await 之前（含 `capture.setPaused(false)`）就分配控制代际；新增“setPaused(false) 被闸门挂起时发出 show”的确定性 API 竞态测试，旧 bg 必须失败并跳过最小化/隐藏。
  - 每服务器一条 native 可见性队列（hide/unhide/activate 按到达顺序执行），临界区内重查代际：bg 先进入 hide 时，后到 show 的 unhide 排在其后执行；show 先到时旧 bg 跳过 hide。新增“hideBrowser 被闸门挂起时发出 show”的测试，最终状态必须可见。
  - 新增 `app-control unhide`（只取消隐藏、不抢焦点）；测试保持 `activate:false` 语义。AppKit 的 hide/unhide 是异步的，helper 进程无法观察，改为：发出请求后用新进程校验状态并做有界重试（实测同一轮 show/bg 不再出现 hidden 状态漂移）。
  - `/api/stop`、`/api/restart`、`/api/extensions/dev`、`/api/inspect` 也接入代际/队列，避免旧任务覆盖更新的意图。

### 最小化根因（实测 Chrome for Testing 153 / macOS 26）

- `show --maximize` 后 `/api/bg`：CDP windowState 很快报 minimized，但 AppKit 从未真正 miniaturize，活动标签 `document.hidden` 保持 false；`NSRunningApplication.hidden=true` 同样不能证明真的已隐藏。
- 干净对照（每次独立 space；应用由 `open -g -j` 隐藏启动）：单次 normal→minimized 失败；完整 normal→minimized 循环跑两次成功；应用前台可见时一次即可。
- 修复：`/api/bg` 先查询 NSRunningApplication 状态；应用隐藏时 `collapseAll` 对每个窗口执行两次 normal→minimized 循环（隐藏状态下重复不可见），可见时一次。`usability.ts` 采集 target/windowId/bounds/native 状态，并逐页断言最小化、`document.hidden === true`、网络轮询持续增长。

### 验证（品牌化 Backlight，fixture 校验 bundle/icon/可执行摘要）

- 命令与结果（本地时间 2026-09-13 18:1x–19:49）：
  - `pnpm -r --if-present run check`：cli + daemon 通过。
  - `bash packages/tray/build.sh`：`Backlight Tray.app` 构建成功。
  - `pnpm --filter @backlight/daemon test:unit`：25/25 通过（本轮新增 `cdp-unit.ts`、`extension-dev-unit.ts`、`proxy-race-unit.ts`；`windows-unit.ts` 增加假性 minimized 与竞态两项；`proxy-race-unit.ts` 现有 3 个确定性竞态用例）；`tests/brand.ts` 11 项通过。
  - `pnpm --filter @backlight/daemon test:integration`（18:48 起）：spike、supervisor、extensions（含双窗口 3/3）、agent、background、usability 全部 PASS（32 条 PASS，无 FAIL）。
  - `pnpm --filter @backlight/daemon test` 完整复跑（P2 第一轮修复前 18:50、第一轮后 19:23:50、第二轮后 19:47:18 CST）：unit 25/25 + 上述全部集成 PASS；日志 `/tmp/backlight-fulltest-p2-*.log`、`/tmp/backlight-final2-*.log`。
  - 6 条 `[Backlight acceptance]` 均指向 `.../Backlight.app/...`，sha256 一致（`bfe18f25…f789`）；运行后无临时测试 daemon/browser 或临时资料目录遗留，未触碰 9333 用户实例。
- 中断记录：首次集成运行挂在 `tests/agent.ts` 的 `Page.captureScreenshot` 超过 6 分钟；主控定位到 `Cdp.send` 的 waiter 注册竞态与角落窗口不可截图，已按上表修复并完整复跑。另清理了一个历史遗留的 `/tmp/bl-probe5` 测试 Chrome（非用户浏览器）。

### 能力边界

- 角落（corner）模式下窗口几乎完全离屏，Chromium 不产生合成帧：默认 `Page.captureScreenshot` 可能永久等待，`fromSurface:false` 也不保证成功。后台截图请使用 capture keep-alive（spike 实测 57–78ms）或可见窗口；`tests/agent.ts` 因此可见启动。
- 物理 Dock 点击/真鼠标激活仍未自动化（不得声称已验证）。
- 日常 daemon 不热加载源码；以上修复需用户下次安全重启 Backlight daemon 后生效，不要在用户使用期间终止其浏览器。
